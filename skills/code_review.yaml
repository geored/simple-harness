name: code_review
description: Reads a source file and reviews it for bugs, style issues, and improvements.
inputs:
  - name: file_path
    type: string
    required: true
skills:
  - name: read_file
    description: Read a file from disk
    inputs:
      - name: path
        type: string
        required: true
    output_schema:
      type: any
